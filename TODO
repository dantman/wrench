- Should we add Object.invert?
Object.invert = function(obj) {
	var o = {};
	for( let k in obj ) o[obj[k]] = k;
	return o;
};
- We have string.contains and array.has, should we pick one name for both instead?
